import {Dimensions} from 'react-native';

export const width = Dimensions.get('window').width;
export const PAGE_WIDTH = width;
export const PAGE_HEIGHT = width * 1.2;
export const ANIMATION_WIDTH = PAGE_WIDTH / 1.5;
export const ANIMATION_HEIGHT = PAGE_HEIGHT / 1.5;
